import systemMonitor from "./systemMonitor"

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [systemMonitor],
}

export default menuItems
